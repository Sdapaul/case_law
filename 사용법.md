# 판례 자동 알림 시스템 — 사용법 가이드

## 목차
1. [프로그램 개요](#1-프로그램-개요)
2. [아키텍처](#2-아키텍처)
3. [파일별 기능 및 목적](#3-파일별-기능-및-목적)
4. [설치 및 초기 설정](#4-설치-및-초기-설정)
5. [실행 방법](#5-실행-방법)
6. [GitHub Actions 자동화](#6-github-actions-자동화)
7. [데이터베이스](#7-데이터베이스)
8. [설정 파일 안내](#8-설정-파일-안내)
9. [보안 분석 및 안전성](#9-보안-분석-및-안전성)
10. [취약점 분석](#10-취약점-분석)
11. [트러블슈팅](#11-트러블슈팅)

---

## 1. 프로그램 개요

### 무엇을 하는 프로그램인가?

**판례 자동 알림 시스템**은 대한민국 법원 판결 정보를 자동으로 수집하여 이메일로 전달하는 Python 자동화 도구입니다.

### 주요 기능

| 기능 | 설명 |
|------|------|
| **자동 수집** | 법제처 공공데이터 API에서 최신 판례 수집 (매일 2회) |
| **중복 방지** | 이미 발송한 판례는 재발송하지 않음 |
| **우선순위 분류** | 금융·형사·기업법 등 44개 주요 법령 관련 판례를 상단 표시 |
| **AI 요약** | Google Gemini가 판례 내용을 2~3문장으로 쉽게 요약 |
| **이메일 발송** | HTML 형식 + 엑셀 첨부파일로 구독자에게 전송 |
| **데이터 저장** | 발송 이력을 SQLite DB에 영구 보관 |
| **엑셀 내보내기** | 원하는 기간의 판례를 .xlsx 파일로 추출 |

### 실행 흐름 요약

```
법제처 API → 신규 판례 필터링 → AI 요약 → 이메일 발송 → DB 저장
```

---

## 2. 아키텍처

### 전체 구조도

```
┌─────────────────────────────────────────────────────────┐
│                    GitHub Actions                         │
│  schedule.yml (매일 07:00, 15:00 KST 자동 실행)           │
│  export_excel.yml (수동 실행, 엑셀 추출)                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                     main.py                              │
│  (전체 파이프라인 조율)                                    │
└──┬────────┬────────┬────────────┬────────────┬──────────┘
   │        │        │            │            │
   ▼        ▼        ▼            ▼            ▼
crawler   dedup  priority    summarizer    emailer
  .py      체크   분류          .py           .py
   │    (sent_   (44개              │           │
   │    cases    법령)              │           │
   ▼    .json)                     ▼           ▼
법제처                         Gemini      Gmail
  API                          API         SMTP
                                            │
                               ┌────────────┘
                               ▼
                          excel_export.py
                               │
                               ▼
                           db.py
                               │
                               ▼
                       cases_db.sqlite
```

### 데이터 흐름 상세

1. **수집 단계**: `crawler.py`가 법제처 API를 호출하여 최근 `days_back`일 내 판례 수집
2. **필터 단계**: `sent_cases.json`과 비교하여 이미 발송한 판례 제외
3. **분류 단계**: `main.py`의 우선순위 법령 목록과 대조하여 중요 판례 분리
4. **요약 단계**: `summarizer.py`가 Gemini API로 각 판례 요약 (API 키 없으면 생략)
5. **발송 단계**: `emailer.py`가 HTML 이메일 + 엑셀 첨부파일 생성 후 Gmail 발송
6. **저장 단계**: `db.py`가 SQLite DB에 발송 이력 기록, `sent_cases.json` 업데이트

---

## 3. 파일별 기능 및 목적

### `main.py` — 메인 오케스트레이터

**역할**: 전체 파이프라인을 조율하는 진입점

**주요 기능**:
- `config.json` 설정 로드
- `sent_cases.json` 중복 목록 로드/저장
- 우선순위 법령 44개 목록 관리
- `--dry-run` 옵션으로 테스트 모드 지원 (이메일 미발송)
- 판례를 우선순위 → 일반 순서로 정렬

**우선순위 법령 목록 (44개)**:
- 금융: 금융회사의 지배구조에 관한 법률, 자본시장과 금융투자업에 관한 법률, 보험업법 등
- 형사: 형법, 특정경제범죄 가중처벌 등에 관한 법률 등
- 기업: 상법, 주식회사 등의 외부감사에 관한 법률 등
- 노동/안전: 근로자퇴직급여 보장법, 중대재해 처벌 등에 관한 법률 등
- 개인정보/IT: 개인정보 보호법, 정보통신망 이용촉진 및 정보보호 등에 관한 법률 등

---

### `crawler.py` — 법제처 API 크롤러

**역할**: 법제처 공공데이터 API에서 판례 수집

**API 주소**: `https://www.law.go.kr/DRF/lawSearch.do`

**주요 기능**:
- 키워드, 법원명, 사건유형 필터링
- 페이지 단위 수집 (페이지당 20건, 최대 5페이지 = 100건)
- 3회 재시도 (지수 백오프: 3초, 6초, 9초)
- `days_back` 기간 내 판례만 선별
- 판례별 정규화 처리 (필드명 통일)

**반환 데이터 구조**:
```python
{
  "seq": "106852",          # API 고유 ID (중복 방지 키)
  "title": "약정금",         # 판례명
  "case_num": "2023다12345", # 사건번호
  "court": "대법원",         # 법원명
  "date": "2024.03.15",     # 선고일
  "summary": "원고는...",    # 판시사항 요약
  "statutes": "민법 제750조", # 관련 법령
  "link": "https://...",    # 원문 링크
  "is_fresh": True          # 최신 여부 (30일 이내)
}
```

---

### `summarizer.py` — AI 요약 엔진

**역할**: Google Gemini 2.0 Flash를 이용해 판례를 쉬운 한국어로 요약

**주요 기능**:
- 배치 처리 (최대 20건/회)
- 분당 15건 제한 준수 (5초 대기)
- 429 오류(쿼터 초과) 시 자동 대기 (60초, 120초)
- 연속 3회 429 발생 시 중단 (일일 한도 초과 판단)
- API 키 없으면 요약 생략 (프로그램은 계속 실행)

**요약 프롬프트**: "판례를 비법률 전문가도 이해할 수 있도록 2~3문장으로 요약해주세요"

**무료 한도**:
- 분당: 15건
- 일일: 1,500건

---

### `emailer.py` — 이메일 발송기

**역할**: HTML 이메일 작성 및 Gmail SMTP를 통한 발송

**이메일 구성**:
- **형식**: HTML (주) + 평문 (대체)
- **첨부**: `.xlsx` 엑셀 파일
- **표시**: 우선순위 판례(★ 표시), 최신 판례([최신] 배지)

**이메일 표 항목**:
- 판례명 (원문 링크 포함)
- 사건번호
- 법원
- 선고일
- AI 요약 (있을 경우)

**발송 방법**:
- 프로토콜: Gmail SMTP SSL (포트 465)
- 인증: 앱 비밀번호 (Google 계정 2단계 인증 필요)

---

### `excel_export.py` — 엑셀 생성기

**역할**: 판례 데이터를 색상 코딩된 `.xlsx` 파일로 생성

**컬럼 순서**: 발송일, 판례명, 사건번호, 법원, 사건유형, 선고일, AI요약, 참조 URL, 우선순위여부, 최신판례여부

**색상 코드**:
- 헤더: 진한 파란색 `#1A56C4`
- 우선순위 판례 행: 연한 파란색 `#DDEAFF`
- 최신 판례 행: 연한 초록색 `#E6FFE6`
- 일반 행: 흰색/연한 파란색 교대 `#F2F6FF`

**추가 기능**:
- 2행 고정(헤더 고정)
- 한국어 폰트(맑은 고딕)
- URL 컬럼 하이퍼링크 처리
- 행 높이: 본문 60px, 헤더 22px

---

### `db.py` — 데이터베이스 레이어

**역할**: SQLite 데이터베이스 읽기/쓰기

**주요 함수**:
- `save_cases(cases)`: 발송 완료 판례 일괄 저장
- `query_cases(start_date, end_date)`: 기간별 판례 조회 (엑셀 추출용)

---

### `config.json` — 런타임 설정

```json
{
  "keywords": [],      // 검색 키워드 (빈 배열 = 전체)
  "court_name": "",    // 법원 필터 (빈 문자열 = 전체)
  "case_type": "",     // 사건유형 필터 (빈 문자열 = 전체)
  "max_pages": 5,      // 크롤링 페이지 수 (1페이지=20건)
  "days_back": 30      // 최근 N일 이내 판례만 수집
}
```

---

### `sent_cases.json` — 중복 방지 목록

```json
["106852", "107029", "95다21365", ...]
```
- 발송 완료된 판례의 `seq` 또는 `case_num` 목록
- 새 판례 발송 후 자동으로 추가됨
- GitHub Actions가 자동 커밋

---

## 4. 설치 및 초기 설정

### 사전 요구사항

- Python 3.11 이상
- Gmail 계정 (2단계 인증 활성화 필요)
- 법제처 API 키 ([오픈API 신청](https://open.law.go.kr))
- Google Gemini API 키 (선택, [Google AI Studio](https://aistudio.google.com))

### 로컬 설치

```bash
# 1. 저장소 클론
git clone https://github.com/YOUR_USERNAME/test_case_law.git
cd test_case_law

# 2. 의존성 설치
pip install -r requirements.txt

# 3. 환경 변수 설정 (.env 파일 생성 또는 직접 설정)
export LAW_API_KEY="your_law_api_key"
export GMAIL_SENDER="your@gmail.com"
export GMAIL_APP_PASSWORD="xxxx xxxx xxxx xxxx"
export RECIPIENT_EMAILS="user1@example.com,user2@example.com"
export GEMINI_API_KEY="AIzaSy..."  # 선택
```

### Gmail 앱 비밀번호 생성 방법

1. Google 계정 → 보안 → 2단계 인증 활성화
2. 보안 → 앱 비밀번호 → 앱 선택: "메일" → 기기: "기타(Windows 컴퓨터)"
3. 생성된 16자리 비밀번호를 `GMAIL_APP_PASSWORD`에 입력

### GitHub Secrets 설정 방법

1. GitHub 저장소 → Settings → Secrets and variables → Actions
2. "New repository secret" 버튼으로 각 환경 변수 추가:
   - `LAW_API_KEY`
   - `GMAIL_SENDER`
   - `GMAIL_APP_PASSWORD`
   - `RECIPIENT_EMAILS`
   - `GEMINI_API_KEY` (선택)

---

## 5. 실행 방법

### 실제 발송

```bash
python main.py
```

### 테스트 모드 (이메일 미발송)

```bash
python main.py --dry-run
```
- 판례 수집 및 요약은 진행하나 이메일 발송 없음
- DB 저장 및 `sent_cases.json` 업데이트 없음
- 어떤 판례가 발송될지 미리 확인 가능

### 엑셀 내보내기 (로컬)

```python
from db import query_cases
from excel_export import generate_excel

cases = query_cases("2025-01-01", "2025-12-31")
excel_bytes = generate_excel(cases, "2025년 판례 목록")

with open("output.xlsx", "wb") as f:
    f.write(excel_bytes)
```

---

## 6. GitHub Actions 자동화

### 정기 발송 워크플로 (`schedule.yml`)

| 항목 | 내용 |
|------|------|
| **자동 실행** | 매일 07:00 KST, 15:00 KST |
| **수동 실행** | Actions 탭 → "Run workflow" 버튼 |
| **dry-run 옵션** | 수동 실행 시 체크박스 선택 가능 |
| **실행 후** | 변경된 `sent_cases.json`, `cases_db.sqlite` 자동 커밋 |
| **커밋 메시지** | `chore: update sent_cases.json + cases_db.sqlite [skip ci]` |
| **타임아웃** | 20분 |

### 엑셀 내보내기 워크플로 (`export_excel.yml`)

| 항목 | 내용 |
|------|------|
| **실행 방법** | Actions 탭 → "Export Excel" → Run workflow |
| **입력값** | start_date (예: 2025-01-01), end_date (예: 2025-12-31) |
| **결과물** | 아티팩트로 .xlsx 파일 다운로드 (30일 보관) |

---

## 7. 데이터베이스

### 파일 위치

`cases_db.sqlite` (프로젝트 루트, 약 32KB, 약 627건)

### 테이블 구조: `sent_cases`

| 컬럼명 | 타입 | 설명 | 예시 |
|--------|------|------|------|
| `id` | INTEGER | 자동 증가 기본키 | 1, 2, 3... |
| `sent_date` | TEXT | 발송일 | 2024-03-15 |
| `seq` | TEXT | API 고유 ID (중복 방지) | 106852 |
| `title` | TEXT | 판례명 | 약정금 |
| `case_num` | TEXT | 사건번호 | 2023다12345 |
| `court` | TEXT | 법원명 | 대법원 |
| `case_type` | TEXT | 사건유형 | 민사 |
| `decision_date` | TEXT | 선고일 | 2024.03.15 |
| `ai_summary` | TEXT | AI 요약 | 원고는... |
| `statutes` | TEXT | 참조 법령 | 민법 제750조 |
| `link` | TEXT | 원문 URL | https://... |
| `is_priority` | INTEGER | 우선순위 여부 | 1 (우선) / 0 (일반) |
| `is_fresh` | INTEGER | 최신 판례 여부 | 1 (최신) / 0 (일반) |

### DB 직접 조회 방법

```bash
# SQLite CLI로 접속
sqlite3 cases_db.sqlite

# 전체 레코드 수 확인
SELECT COUNT(*) FROM sent_cases;

# 최근 10건 조회
SELECT sent_date, title, court, decision_date 
FROM sent_cases 
ORDER BY id DESC 
LIMIT 10;

# 우선순위 판례만 조회
SELECT title, court, decision_date 
FROM sent_cases 
WHERE is_priority = 1;

# 특정 기간 조회
SELECT * FROM sent_cases 
WHERE sent_date BETWEEN '2025-01-01' AND '2025-03-31';
```

### `sent_cases.json`과 DB의 차이

| 항목 | `sent_cases.json` | `cases_db.sqlite` |
|------|-------------------|--------------------|
| **목적** | 중복 발송 방지 | 이력 조회 및 엑셀 추출 |
| **내용** | case ID 목록만 | 판례 전체 정보 |
| **형식** | JSON 배열 | SQLite 테이블 |
| **크기** | ~9KB | ~32KB |

---

## 8. 설정 파일 안내

### `config.json` 수정 예시

**특정 법원 판례만 수집**:
```json
{
  "keywords": [],
  "court_name": "대법원",
  "case_type": "",
  "max_pages": 3,
  "days_back": 7
}
```

**키워드 기반 수집**:
```json
{
  "keywords": ["손해배상", "계약"],
  "court_name": "",
  "case_type": "민사",
  "max_pages": 5,
  "days_back": 30
}
```

**적극적 수집 (더 많은 판례)**:
```json
{
  "keywords": [],
  "court_name": "",
  "case_type": "",
  "max_pages": 10,
  "days_back": 60
}
```

---

## 9. 보안 분석 및 안전성

### 인증 정보 관리

| 항목 | 방법 | 안전 여부 |
|------|------|-----------|
| API 키 | GitHub Secrets (암호화 저장) | ✅ 안전 |
| Gmail 비밀번호 | 앱 비밀번호 사용 (2FA 필요) | ✅ 안전 |
| DB 파일 | 공개 저장소에 포함 | ⚠️ 주의 (개인정보 없음) |

### 코드 보안 수준

| 항목 | 현황 | 평가 |
|------|------|------|
| 환경 변수 분리 | 모든 시크릿을 환경 변수로 처리 | ✅ 양호 |
| 하드코딩 시크릿 | 코드 내 API 키 없음 | ✅ 양호 |
| HTTPS 사용 | 법제처 API, Gemini API 모두 HTTPS | ✅ 양호 |
| SMTP 암호화 | SSL 포트 465 사용 | ✅ 양호 |
| 입력값 검증 | config.json 스키마 검증 없음 | ⚠️ 개선 여지 |
| SQL Injection | 파라미터 바인딩 사용 (`?` 플레이스홀더) | ✅ 안전 |
| 재시도 로직 | 지수 백오프로 API 남용 방지 | ✅ 양호 |

### Gmail 앱 비밀번호 보안

- 일반 Gmail 비밀번호 대신 **앱 전용 비밀번호** 사용
- 앱 비밀번호는 해당 앱에서만 유효, 언제든 취소 가능
- Google 계정 2단계 인증으로 추가 보호

### DB 파일 공개 저장소 주의사항

`cases_db.sqlite`는 공개 저장소에 포함되어 있습니다:
- 저장 데이터: 법원 공개 판례 정보만 (개인정보 없음)
- 판례 원문은 법제처에서 이미 공개된 정보
- AI 요약은 추가 가공이나 별도 민감 정보 없음
- 결론: **현재 저장 데이터 기준 공개 적합**

---

## 10. 취약점 분석

### 현재 확인된 취약점 및 개선 방안

#### [낮음] config.json 입력값 검증 부재

**위험**: 잘못된 `config.json` 작성 시 오류 발생 가능
```json
// 문제가 되는 예시
{ "max_pages": "five", "days_back": -1 }
```
**현재**: 타입 오류 또는 잘못된 동작
**개선안**: JSON Schema 검증 또는 타입 캐스팅 추가

#### [낮음] beautifulsoup4/lxml 미사용 의존성

**위험**: 알려진 취약점 발생 시 불필요한 공격 표면
**현재**: `requirements.txt`에 포함되나 코드에서 미사용
**개선안**: `requirements.txt`에서 제거

#### [정보] DB 트랜잭션 미관리

**위험**: 발송 중 오류 시 DB와 `sent_cases.json` 불일치 가능성
**현재**: DB 저장과 JSON 업데이트가 별개로 실행
**개선안**: 원자적 처리 (둘 다 성공 또는 둘 다 실패)

#### [낮음] 이메일 수신자 목록 환경 변수 노출

**위험**: GitHub Actions 로그에서 이메일 주소 부분 노출 가능
**현재**: 실행 로그에 일부 출력
**개선안**: 로그에서 이메일 마스킹 처리

#### [정보] Gemini API 응답 신뢰

**위험**: AI 요약 내용에 대한 검증 없음
**현재**: AI 응답을 그대로 이메일에 포함
**평가**: 내부 운영 도구이므로 현재 수준 적절

### 공격 시나리오별 평가

| 시나리오 | 위험도 | 현재 방어 |
|----------|--------|-----------|
| API 키 탈취 | 높음 | GitHub Secrets 암호화로 보호 |
| SQL Injection | 낮음 | 파라미터 바인딩 사용 |
| 이메일 스팸 발송 | 낮음 | 중복 방지 로직 존재 |
| 대량 API 호출 | 낮음 | max_pages 설정으로 제한 |
| Gemini API 쿼터 소진 | 낮음 | 429 감지 후 자동 중단 |

---

## 11. 트러블슈팅

### 이메일이 발송되지 않을 때

1. GitHub Secrets에 `GMAIL_SENDER`, `GMAIL_APP_PASSWORD`, `RECIPIENT_EMAILS` 설정 확인
2. Gmail 2단계 인증 활성화 여부 확인
3. 앱 비밀번호(16자리)가 일반 비밀번호가 아닌지 확인
4. `--dry-run` 모드로 실행 중인지 확인

### 판례가 수집되지 않을 때

1. `LAW_API_KEY` 유효성 확인 (법제처 오픈API 포털에서 검증)
2. `config.json`의 `days_back` 값 확인 (너무 작으면 결과 없음)
3. `sent_cases.json`에 이미 모두 포함된 경우 — 신규 판례 없음이 정상

### AI 요약이 없을 때

- `GEMINI_API_KEY` 미설정 시 정상 동작 (요약 없이 발송)
- 일일 한도(1,500건) 초과 시 다음 날 자동 재개
- Gemini API가 비활성화된 경우 Google AI Studio에서 확인

### `sent_cases.json` 초기화 방법

주의: 초기화 시 모든 판례가 "신규"로 처리되어 대량 발송 위험

```bash
# 백업 후 초기화
cp sent_cases.json sent_cases.backup.json
echo "[]" > sent_cases.json
```

### GitHub Actions 실행 실패 시

1. Actions 탭에서 실패한 워크플로 클릭
2. 로그에서 오류 메시지 확인
3. 주요 원인:
   - Secrets 미설정
   - 네트워크 타임아웃
   - API 한도 초과

---

## 의존성 목록

| 패키지 | 버전 | 용도 |
|--------|------|------|
| requests | 2.32.3 | HTTP API 호출 |
| openpyxl | 3.1.5 | 엑셀 파일 생성 |
| beautifulsoup4 | 4.12.3 | HTML 파싱 (미사용) |
| lxml | 5.3.0 | XML 파서 (미사용) |

---

*마지막 업데이트: 2026-04-24*
