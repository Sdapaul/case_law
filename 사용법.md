# 판례 자동 알림 시스템 — 사용법 및 기술 가이드

> 처음 이 프로젝트를 접하는 분을 위한 완전 설명서입니다.

---

## 목차

1. [프로그램 개요](#1-프로그램-개요)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [파일별 기능 및 목적](#3-파일별-기능-및-목적)
4. [설치 및 초기 설정](#4-설치-및-초기-설정)
5. [실행 방법](#5-실행-방법)
6. [GitHub Actions 자동화](#6-github-actions-자동화)
7. [데이터베이스 상세 안내](#7-데이터베이스-상세-안내)
8. [설정 파일 상세 안내](#8-설정-파일-상세-안내)
9. [이메일 수신 결과 설명](#9-이메일-수신-결과-설명)
10. [보안 안전성 분석](#10-보안-안전성-분석)
11. [취약점 분석 및 개선 방안](#11-취약점-분석-및-개선-방안)
12. [의존성 패키지](#12-의존성-패키지)
13. [트러블슈팅](#13-트러블슈팅)

---

## 1. 프로그램 개요

### 이 프로그램은 무엇인가?

**판례 자동 알림 시스템**은 대한민국 법제처의 공공 API를 통해 최신 법원 판결문(판례)을 자동으로 수집하여 이메일로 발송하는 Python 자동화 시스템입니다.

법률 담당자, 컴플라이언스 팀, 법무팀이 매일 수동으로 판례를 검색하는 번거로움 없이 **자동으로 중요 판례를 받아볼 수 있습니다**.

### 핵심 기능 요약

| 기능 | 설명 |
|------|------|
| **자동 수집** | 법제처 공공데이터 API에서 최신 판례 자동 수집 (매일 2회) |
| **중복 방지** | 이미 발송한 판례는 재발송하지 않음 |
| **우선순위 분류** | 금융·형사·기업·개인정보 등 주요 법령 관련 판례를 상단에 표시 |
| **AI 요약** | Google Gemini가 법률 용어를 일반인도 이해하기 쉽게 2~3문장 요약 |
| **이메일 발송** | HTML 형식의 보기 좋은 이메일 + 엑셀 첨부파일로 전송 |
| **이력 저장** | 모든 발송 판례를 SQLite DB에 영구 보관 |
| **엑셀 추출** | 원하는 날짜 범위로 전체 이력을 .xlsx 파일로 추출 |

### 처리 흐름 한눈에 보기

```
법제처 API → 판례 수집 → 중복 제거 → 우선순위 정렬
         → AI 요약 → 이메일 발송 → DB 저장
```

---

## 2. 시스템 아키텍처

### 전체 구조도

```
┌──────────────────────────────────────────────────────────────┐
│                      GitHub Actions (CI/CD)                   │
│                                                              │
│  schedule.yml                  export_excel.yml              │
│  ┌─────────────────────┐       ┌────────────────────────┐    │
│  │ 매일 07:00 KST 자동  │       │ 수동 실행              │    │
│  │ 매일 15:00 KST 자동  │       │ start_date ~ end_date  │    │
│  │ 수동 실행 가능       │       │ .xlsx 아티팩트 생성    │    │
│  └──────────┬──────────┘       └────────────┬───────────┘    │
└─────────────┼──────────────────────────────┼────────────────┘
              │                               │
              ▼                               ▼
     ┌────────────────┐             ┌─────────────────┐
     │    main.py     │             │    db.py        │
     │  (오케스트레이터)│             │  query_cases()  │
     └──┬──┬──┬──┬───┘             └────────┬────────┘
        │  │  │  │                           │
        │  │  │  └── sent_cases.json         │
        │  │  │      (중복 방지 목록)          ▼
        │  │  │                    ┌──────────────────┐
        │  │  └── PRIORITY_LAWS   │ excel_export.py  │
        │  │      (44개 법령 집합) │ generate_excel() │
        │  │                      └──────────────────┘
        │  └── summarizer.py
        │       │
        │       ▼ HTTP POST
        │  Google Gemini API
        │  (AI 요약, 선택)
        │
        └── crawler.py
             │
             ▼ HTTP GET
        법제처 Open API
        www.law.go.kr
             │
             ▼
        ┌──────────┐   ┌──────────────┐
        │emailer.py│   │   db.py      │
        │  발송    │   │  save_cases()│
        └────┬─────┘   └──────┬───────┘
             │                │
             ▼                ▼
        Gmail SMTP       cases_db.sqlite
        (포트 465 SSL)   (영구 이력 저장)
```

### 컴포넌트 간 의존 관계

```
main.py
  ├── crawler.py      (판례 수집)
  ├── summarizer.py   (AI 요약)
  ├── emailer.py      (이메일 발송)
  │     └── excel_export.py  (첨부파일 생성)
  └── db.py           (이력 저장)
```

### 기술 스택

| 구분 | 기술 |
|------|------|
| 언어 | Python 3.11 |
| DB | SQLite 3 (내장 라이브러리) |
| 이메일 | smtplib + MIME (내장) |
| HTTP | requests 라이브러리 |
| 엑셀 | openpyxl |
| AI | Google Gemini 2.0 Flash API |
| 자동화 | GitHub Actions |
| 데이터 소스 | 법제처 국가법령정보 Open API |

---

## 3. 파일별 기능 및 목적

### `main.py` — 메인 오케스트레이터 (190줄)

**역할**: 전체 파이프라인을 순서대로 실행하는 진입점

**우선순위 법령 목록 (44개 포함)**:
- 금융: 자본시장법, 금융소비자보호법, 보험업법, 은행법, 전자금융거래법 등
- 기업: 상법, 외부감사법, 공정거래법, 하도급법 등
- 형사: 형법, 특정경제범죄법, 부정경쟁방지법 등
- 노동/안전: 근로자퇴직급여법, 중대재해처벌법 등
- 개인정보/디지털: 개인정보보호법, 정보통신망법, 인공지능법 등

**주요 동작 순서**:
1. `config.json` 로드
2. `sent_cases.json` 로드 (발송 완료 ID 집합)
3. `crawler.py`로 판례 수집
4. 이미 발송된 판례 제외
5. 우선순위 법령 → 일반 순서로 정렬 (각 그룹 내 최신순)
6. `summarizer.py`로 AI 요약 추가
7. 이메일 발송 (`emailer.py`) 또는 `--dry-run` 시 콘솔 출력
8. `db.py`로 DB 저장, `sent_cases.json` 갱신

---

### `crawler.py` — 법제처 API 크롤러 (143줄)

**역할**: 법제처 공공데이터 Open API에서 판례 수집 및 정규화

**API 엔드포인트**:
```
GET https://www.law.go.kr/DRF/lawSearch.do
  ?OC={LAW_API_KEY}
  &target=prec
  &type=JSON
  &query={keywords}
  &court={court_name}
  &precSort=ddes           (선고일 최신순)
  &display=20&page={n}
```

**수집 결과 정규화 예시**:
```python
{
  "seq": "106852",            # 판례일련번호 (중복 방지 키)
  "title": "약정금",           # 사건명
  "case_num": "2023다12345",  # 사건번호
  "court": "대법원",           # 법원명
  "case_type": "민사",         # 사건종류
  "date": "2024.03.15",       # 선고일 (YYYY.MM.DD)
  "summary": "원고는...",      # 판시사항 앞 500자
  "statutes": "민법 제750조",  # 참조조문 (우선순위 매칭용)
  "link": "https://...",      # law.go.kr 원문
  "is_fresh": True            # 선고일 ≤ days_back 이내
}
```

**안정성 처리**:
- 3회 재시도, 지수 백오프 (3s → 6s → 9s)
- User-Agent 헤더 스푸핑 (Chrome 124)
- 페이지 < 20건 시 마지막 페이지로 판단해 중단
- `days_back` 기준 날짜 필터 (오래된 판례 제외)

---

### `summarizer.py` — AI 요약 엔진 (97줄)

**역할**: Google Gemini 2.0 Flash로 판례를 비법률 전문가용 요약문 생성

**처리 방식**:
- 입력: `판시사항` (없으면 `판례명 + 사건유형`)
- 프롬프트: "법률 비전문가도 이해하기 쉽게 2~3문장으로 요약, 핵심 쟁점과 판단 결론 포함"
- 출력: `case["summary"]` 필드 직접 수정 (in-place)

**속도 제한 준수**:
- 건당 5초 대기 (분당 15건 무료 한도 준수)
- HTTP 429 발생 시: 60초 대기 후 재시도 → 120초 대기 → 3회 연속 시 중단
- 최대 처리: 20건/회 (일일 1,500건 안전 범위)

**API 미설정 시**: 요약 없이 원본 `summary` 그대로 유지, 프로그램 정상 계속

---

### `emailer.py` — 이메일 발송기 (234줄)

**역할**: Gmail SMTP로 HTML + 평문 + 엑셀 첨부 이메일 발송

**이메일 구성**:
- 제목: `[판례 알림] 2024년 03월 15일 09:00 — 5건 새 판례`
- 형식: multipart/alternative (HTML 주, 평문 대체)
- 첨부: `판례목록_YYYYMMDD.xlsx`

**HTML 이메일 디자인**:
- 헤더 배너: 파란색 (`#1A56C4`)
- 우선순위 판례 행: 연한 파란색 (`#DDEAFF`) + ★ 아이콘
- 최신 판례 행: 연한 초록색 (`#E6FFE6`) + [최신] 배지
- AI 요약: 파란 테두리 박스
- 원문 링크: 판례명에 하이퍼링크

**SMTP 연결**:
```
서버: smtp.gmail.com
포트: 465 (SSL/TLS — 587 아님)
인증: GMAIL_SENDER + GMAIL_APP_PASSWORD
```

**오류 처리**: Excel 첨부 실패해도 이메일 발송 계속 (로그 경고만)

---

### `excel_export.py` — 엑셀 생성기 (108줄)

**역할**: openpyxl로 색상 코딩된 .xlsx 파일 생성 (바이너리 반환)

**10개 컬럼 순서**:

| 순서 | 컬럼명 | 설명 |
|------|--------|------|
| 1 | 발송일 | 이메일 발송 날짜 (YYYY-MM-DD) |
| 2 | 판례명 | 사건명 |
| 3 | 사건번호 | 법원 공식 사건번호 |
| 4 | 법원 | 법원명 |
| 5 | 사건유형 | 민사/형사/행정 등 |
| 6 | 선고일 | 판결 선고 날짜 |
| 7 | AI요약 | Gemini 요약문 |
| 8 | 참조 URL | law.go.kr 원문 링크 (하이퍼링크) |
| 9 | 우선순위여부 | ★ 우선순위 또는 공백 |
| 10 | 최신판례여부 | ✓ 최신 또는 공백 |

**스타일**:
- 폰트: 맑은 고딕 10~11pt
- 헤더 고정: 2행 고정 (스크롤 시 헤더 유지)
- 행 높이: 본문 60px
- 모든 셀 테두리: 얇은 회색 선

---

### `db.py` — 데이터베이스 레이어 (83줄)

**역할**: SQLite 데이터베이스 읽기·쓰기 인터페이스

**함수 2개**:
- `save_cases(cases: list)`: 발송 판례 일괄 INSERT
- `query_cases(start_date: str, end_date: str)`: 기간별 조회 (엑셀 추출용)

**SQL Injection 방지**: 모든 쿼리 `?` 파라미터 바인딩 사용

---

### `config.json` — 검색 파라미터

```json
{
  "keywords": [],      // 검색어 (빈 배열 = 전체 판례)
  "court_name": "",    // 특정 법원 필터 (빈 문자열 = 전체)
  "case_type": "",     // 사건유형 필터 (빈 문자열 = 전체)
  "max_pages": 5,      // 가져올 페이지 수 (1페이지=20건, 5페이지=최대 100건)
  "days_back": 30      // 최근 며칠 이내 판례만 수집
}
```

---

### `sent_cases.json` — 중복 방지 목록

```json
["106852", "107029", "2023다12345", "95다21365", ...]
```
- 발송 완료된 판례의 `seq`(판례일련번호) 또는 `case_num`(사건번호) 목록
- 프로그램 실행 후 자동 갱신
- GitHub Actions가 자동 커밋 (매 실행 후)

---

## 4. 설치 및 초기 설정

### 사전 요구사항

- **Python 3.11** 이상 설치
- **Gmail 계정** — 2단계 인증 활성화 필수
- **법제처 API 키** — 무료 발급: [법제처 오픈API 포털](https://open.law.go.kr)
- **Google Gemini API 키** — 선택사항, 무료: [Google AI Studio](https://aistudio.google.com)

### 의존성 설치

```bash
pip install -r requirements.txt
```

### Gmail 앱 비밀번호 발급 방법

1. Google 계정 로그인 → 우상단 아이콘 → **계정 관리**
2. **보안** 탭 → **2단계 인증** 활성화
3. 보안 탭 → **앱 비밀번호** (검색해서 찾기)
4. 앱 선택: "기타(맞춤 이름)" → 이름 입력 (예: "판례알림")
5. 생성된 **16자리 비밀번호** 복사 → `GMAIL_APP_PASSWORD`에 사용
   - 예: `abcd efgh ijkl mnop` (공백 포함 그대로 사용)

### 환경 변수 설정 (로컬 실행 시)

```bash
export LAW_API_KEY="your_api_key_here"
export GMAIL_SENDER="sender@gmail.com"
export GMAIL_APP_PASSWORD="abcd efgh ijkl mnop"
export RECIPIENT_EMAILS="user1@example.com,user2@example.com"
export GEMINI_API_KEY="AIzaSy..."   # 선택
```

### GitHub Secrets 설정 방법

1. GitHub 저장소 페이지 → **Settings** 탭
2. 좌측 메뉴 → **Secrets and variables** → **Actions**
3. **New repository secret** 버튼으로 아래 항목 추가:

| Secret 이름 | 값 예시 |
|------------|--------|
| `LAW_API_KEY` | `abcdef1234567890...` |
| `GMAIL_SENDER` | `yourname@gmail.com` |
| `GMAIL_APP_PASSWORD` | `abcd efgh ijkl mnop` |
| `RECIPIENT_EMAILS` | `user1@company.com,user2@company.com` |
| `GEMINI_API_KEY` | `AIzaSy...` (선택) |

---

## 5. 실행 방법

### 일반 실행 (이메일 발송)

```bash
python main.py
```

### 테스트 모드 (이메일 미발송)

```bash
python main.py --dry-run
```

`--dry-run` 모드에서는:
- 판례 수집 및 AI 요약은 정상 실행
- 이메일 발송 없음
- DB 저장 및 `sent_cases.json` 업데이트 없음
- 콘솔에 발송 예정 판례 목록 출력

### 엑셀 내보내기 (로컬에서)

```python
from db import query_cases
from excel_export import generate_excel

cases = query_cases("2025-01-01", "2025-12-31")
excel_bytes = generate_excel(cases, "2025년 판례목록")

with open("판례목록_2025.xlsx", "wb") as f:
    f.write(excel_bytes)
```

---

## 6. GitHub Actions 자동화

### 정기 발송 (`schedule.yml`)

```yaml
on:
  schedule:
    - cron: '0 22 * * *'   # 매일 07:00 KST (UTC+9)
    - cron: '0 6 * * *'    # 매일 15:00 KST (UTC+9)
  workflow_dispatch:
    inputs:
      dry_run:
        type: boolean       # 수동 실행 시 dry-run 여부 선택 가능
```

**실행 후 자동 커밋**:
- 변경된 `sent_cases.json`, `cases_db.sqlite`를 자동으로 커밋
- 커밋 메시지: `chore: update sent_cases.json + cases_db.sqlite [skip ci]`
- `[skip ci]` 태그로 무한 루프 방지

### 엑셀 추출 (`export_excel.yml`)

1. GitHub Actions 탭 → **Export Excel** 워크플로 선택
2. **Run workflow** 클릭
3. `start_date`, `end_date` 입력 (형식: `YYYY-MM-DD`)
4. 실행 완료 후 **Artifacts** 에서 .xlsx 다운로드 (30일 보관)

---

## 7. 데이터베이스 상세 안내

### 파일 정보

| 항목 | 내용 |
|------|------|
| 파일명 | `cases_db.sqlite` |
| 위치 | 프로젝트 루트 |
| 크기 | 약 32KB (약 600건) |
| 엔진 | SQLite 3 (Python 내장) |
| 인코딩 | UTF-8 |

### 테이블: `sent_cases`

| 컬럼 | 타입 | 설명 | 예시 값 |
|------|------|------|---------|
| `id` | INTEGER PK | 자동 증가 | 1, 2, 3 |
| `sent_date` | TEXT | 이메일 발송 날짜 | 2024-03-15 |
| `seq` | TEXT | 법제처 API 고유 ID | 106852 |
| `title` | TEXT | 사건명 | 약정금 |
| `case_num` | TEXT | 사건번호 | 2023다12345 |
| `court` | TEXT | 법원명 | 대법원 |
| `case_type` | TEXT | 사건종류 | 민사 |
| `decision_date` | TEXT | 선고일 | 2024.03.15 |
| `ai_summary` | TEXT | Gemini AI 요약문 | 원고는 피고와... |
| `statutes` | TEXT | 참조 법령 목록 | 민법 제750조 |
| `link` | TEXT | 원문 URL | https://www.law.go.kr/... |
| `is_priority` | INTEGER | 우선순위 법령 여부 | 1 또는 0 |
| `is_fresh` | INTEGER | 선고일 ≤ 30일 여부 | 1 또는 0 |

### DB 직접 조회 방법

```bash
# DB 접속
sqlite3 cases_db.sqlite

# 전체 건수
SELECT COUNT(*) FROM sent_cases;

# 최근 10건 조회
SELECT sent_date, title, court, decision_date
FROM sent_cases
ORDER BY id DESC LIMIT 10;

# 우선순위 판례만 조회
SELECT title, court, decision_date
FROM sent_cases WHERE is_priority = 1;

# 특정 기간 조회 (발송 날짜 기준)
SELECT * FROM sent_cases
WHERE sent_date BETWEEN '2025-01-01' AND '2025-03-31';

# 법원별 판례 수
SELECT court, COUNT(*) as cnt
FROM sent_cases GROUP BY court ORDER BY cnt DESC;
```

### `sent_cases.json`과 DB 비교

| 항목 | `sent_cases.json` | `cases_db.sqlite` |
|------|-------------------|-------------------|
| 목적 | 빠른 중복 방지 체크 | 전체 이력 조회 및 엑셀 추출 |
| 내용 | case ID 배열만 | 판례 전체 정보 |
| 크기 | ~9KB | ~32KB |
| 쿼리 | O(1) set lookup | SQL 쿼리 |

**중요**: 두 파일은 항상 함께 갱신됩니다. 하나만 수동으로 수정하면 불일치가 발생합니다.

---

## 8. 설정 파일 상세 안내

### `config.json` 수정 예시

**대법원 판례만 매일 수집**:
```json
{
  "keywords": [],
  "court_name": "대법원",
  "case_type": "",
  "max_pages": 3,
  "days_back": 7
}
```

**손해배상 민사 사건만 수집**:
```json
{
  "keywords": ["손해배상"],
  "court_name": "",
  "case_type": "민사",
  "max_pages": 5,
  "days_back": 30
}
```

**전체 판례 폭넓게 수집**:
```json
{
  "keywords": [],
  "court_name": "",
  "case_type": "",
  "max_pages": 10,
  "days_back": 60
}
```

### 설정값 영향도

| 설정 | 증가 시 효과 | 주의사항 |
|------|------------|---------|
| `max_pages` ↑ | 더 많은 판례 수집 | API 호출 횟수 비례 증가 |
| `days_back` ↑ | 더 오래된 판례 포함 | 초기 실행 시 대량 발송 주의 |
| `keywords` 추가 | 특정 주제 집중 | 빈 배열이 가장 많은 결과 |

---

## 9. 이메일 수신 결과 설명

### 이메일 구성 요소

```
제목: [판례 알림] 2024년 03월 15일 09:00 — 5건 새 판례
       └ 발송 날짜와 시간, 새 판례 수

본문 헤더: 검색 조건 (키워드, 법원, 수신자 목록)

판례 표:
  ┌─────────────────────────────────────────────┐
  │ ★ [우선순위] 판례명 (링크)    (파란 배경)    │
  │   [최신] 배지, 선고일, 법원                  │
  │   💬 AI 요약: 원고는 피고와의 계약에서...    │
  ├─────────────────────────────────────────────┤
  │   일반 판례명 (링크)          (흰 배경)      │
  │   선고일, 법원                               │
  └─────────────────────────────────────────────┘

첨부파일: 판례목록_20240315.xlsx
```

### 배지/표시 의미

| 표시 | 의미 |
|------|------|
| ★ (파란 배경) | 우선순위 법령 관련 판례 (상단 표시) |
| [최신] (초록 배경) | 선고일이 30일 이내인 판례 |
| 💬 AI 요약 | Gemini가 생성한 쉬운 요약문 |
| 원문 링크 | 판례명 클릭 시 법제처 원문 페이지 이동 |

---

## 10. 보안 안전성 분석

### 인증 정보 보안

| 항목 | 관리 방법 | 평가 |
|------|-----------|------|
| API 키 전체 | GitHub Secrets (AES-256 암호화 저장) | ✅ 안전 |
| Gmail 비밀번호 | 앱 비밀번호 사용 (2FA 필수) | ✅ 안전 |
| 수신자 이메일 | GitHub Secrets | ✅ 안전 |
| DB 파일 | 공개 저장소 포함 | ⚠️ 아래 참고 |

### DB 파일 공개 저장소 포함 여부 평가

`cases_db.sqlite`는 공개 저장소에 커밋되어 있습니다:
- 저장 내용: 법제처가 **이미 공개한** 판례 정보
- 판례는 대한민국 공공데이터 — 개인정보 없음
- AI 요약문은 공개 판례의 가공 정보
- **결론**: 현재 저장 데이터 기준 공개 저장소에 포함 적합

### 코드 보안 항목별 평가

| 항목 | 현황 | 평가 |
|------|------|------|
| 비밀 정보 하드코딩 | 없음, 전부 환경 변수 | ✅ 안전 |
| HTTPS 사용 | 법제처 API, Gemini API 모두 HTTPS | ✅ 양호 |
| SMTP 암호화 | SSL 포트 465 사용 | ✅ 양호 |
| SQL Injection | `?` 파라미터 바인딩 | ✅ 안전 |
| 과도한 API 호출 | max_pages 설정, 지수 백오프 | ✅ 양호 |
| XSS | 이메일 내용은 법제처 공공 데이터 | ✅ 해당 없음 |
| 입력값 검증 | config.json 스키마 검증 없음 | ⚠️ 개선 여지 |

---

## 11. 취약점 분석 및 개선 방안

### [낮음] config.json 입력값 검증 부재

**설명**: `config.json`을 잘못 작성해도 오류 메시지 불명확

```json
// 문제 예시
{ "max_pages": "five", "days_back": -1 }
```

**현재 동작**: 타입 오류 또는 의도치 않은 동작 (음수 days_back 등)

**개선 방안**:
```python
# main.py에 검증 추가 예시
assert isinstance(cfg["max_pages"], int) and cfg["max_pages"] > 0
assert isinstance(cfg["days_back"], int) and cfg["days_back"] > 0
```

---

### [낮음] 미사용 의존성 존재

**설명**: `requirements.txt`에 `beautifulsoup4`, `lxml` 포함되어 있으나 코드에서 미사용

**위험**: 향후 이 패키지들에 알려진 취약점 발생 시 불필요한 공격 표면 형성

**개선 방안**: `requirements.txt`에서 두 패키지 제거
```
# 제거 대상
beautifulsoup4==4.12.3
lxml==5.3.0
```

---

### [정보] DB와 JSON 비원자적 갱신

**설명**: DB 저장 (`db.save_cases()`)과 JSON 갱신 (`sent_cases.json`)이 별도로 실행

**시나리오**: DB 저장 성공 → JSON 갱신 전 프로세스 종료 → 다음 실행 시 재발송 가능성

**현재 영향**: 낮음 (발송 중단 시에만 발생, GitHub Actions는 안정적)

**개선 방안**: 두 작업을 하나의 트랜잭션처럼 처리하거나, `sent_cases.json`을 제거하고 DB만 사용

---

### [정보] 실행 로그에 이메일 주소 부분 노출

**설명**: GitHub Actions 실행 로그에 수신자 이메일이 일부 출력될 수 있음

**현재 영향**: 저장소가 공개인 경우 로그 열람자에게 노출 가능

**개선 방안**: 이메일 출력 시 마스킹 처리
```python
# 예: user@example.com → u***@example.com
```

---

### 공격 시나리오별 위험도 평가

| 시나리오 | 위험도 | 현재 방어 상태 |
|----------|--------|--------------|
| GitHub Secrets 탈취 | 높음 | GitHub 플랫폼 수준 암호화 |
| SQL Injection 공격 | 낮음 | 파라미터 바인딩으로 차단 |
| 이메일 스팸 대량 발송 | 낮음 | 중복 방지 + 수동 설정 수신자 |
| 법제처 API 과도 호출 | 낮음 | max_pages 제한 |
| Gemini API 쿼터 소진 | 낮음 | 429 감지 후 자동 중단 |
| .env 파일 커밋 | 중간 | .gitignore에 포함됨 |

---

## 12. 의존성 패키지

```
requests==2.32.3      # HTTP 클라이언트 (법제처 API, Gemini API 호출)
openpyxl==3.1.5       # Excel .xlsx 생성
beautifulsoup4==4.12.3  # HTML 파싱 (현재 미사용)
lxml==5.3.0           # XML 파서 (현재 미사용)
```

**런타임 내장 라이브러리** (설치 불필요):
- `smtplib`, `email` — 이메일 발송
- `sqlite3` — 데이터베이스
- `json`, `os`, `datetime`, `argparse` — 기타

---

## 13. 트러블슈팅

### 이메일이 발송되지 않을 때

1. `GMAIL_APP_PASSWORD`가 일반 비밀번호가 아닌 **앱 비밀번호(16자리)**인지 확인
2. Gmail 2단계 인증 활성화 여부 확인
3. `GMAIL_SENDER`, `RECIPIENT_EMAILS` Secrets 설정 확인
4. `--dry-run` 플래그가 붙어 있지 않은지 확인

### 판례가 수집되지 않을 때

1. `LAW_API_KEY` 유효성 확인 (법제처 포털에서 로그인 후 키 상태 확인)
2. `config.json`의 `days_back` 값 확인 (1~7이면 판례 없을 수 있음)
3. `sent_cases.json`에 이미 모든 판례가 포함된 경우 정상 (새 판례 없음)

### AI 요약이 생략될 때

- `GEMINI_API_KEY` 미설정 → 정상 동작 (요약 없이 발송)
- 일일 한도(1,500건) 초과 → 다음 날 자동 재개
- Google AI Studio에서 API 사용량 확인: [aistudio.google.com](https://aistudio.google.com)

### sent_cases.json 초기화 (주의)

> ⚠️ 초기화 시 모든 기존 판례가 "신규"로 처리되어 **대량 발송** 발생 가능

```bash
cp sent_cases.json sent_cases.backup.json  # 반드시 백업 먼저
echo "[]" > sent_cases.json
```

### GitHub Actions 실패 시

1. Actions 탭 → 실패한 워크플로 클릭 → 로그 확인
2. 주요 원인:
   - Secrets 미설정 또는 오타
   - 법제처 API 서버 일시 장애 (재실행으로 해결)
   - Gemini API 한도 초과 (다음 날 자동 재개)
   - 커밋 권한 오류 (`contents: write` 권한 확인)

---

*마지막 업데이트: 2026-04-24*
